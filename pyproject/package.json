{
  "name": "doctools",
  "version": "0.3.0",
  "private": true,
  "files": [],
  "scripts": {
    "build": "python -m hatch clean && python -m hatch build -t sdist -t wheel -t custom",
    "test:pytest": "nx run mdserver:build && python -m pytest",
    "lint:black": "python -m black --check src tests",
    "lint:ruff": "python -m ruff check src tests",
    "typecheck:pyright": "pyright src",
    "release": "python -m hatch publish pyproject/dist"
  },
  "devDependencies": {
    "mdserver": "workspace:^"
  },
  "nx": {
    "$schema": "https://cdn.jsdelivr.net/npm/nx@16.7.0/schemas/project-schema.json",
    "targets": {
      "build": {
        "inputs": [
          ":default",
          ":python"
        ]
      }
    }
  }
}
