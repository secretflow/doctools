{
  "name": "esm-serve-pyo3",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "files": [],
  "scripts": {
    "format:black": "python -m black --check .",
    "lint:ruff": "python -m ruff check",
    "test:pytest": "python -m pytest",
    "typecheck:pyright": "pyright",
    "dev:maturin": "python -m maturin develop --skip-install",
    "build:maturin": "VIRTUAL_ENV=../../.venv python -m maturin build --release --out ./dist",
    "publish:maturin": "python -m maturin publish --out ./dist"
  },
  "devDependencies": {}
}
